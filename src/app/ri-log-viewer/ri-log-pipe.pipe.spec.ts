import { RiLogPipePipe } from './ri-log-pipe.pipe';

describe('RiLogPipePipe', () => {
  it('create an instance', () => {
    const pipe = new RiLogPipePipe();
    expect(pipe).toBeTruthy();
  });
});
